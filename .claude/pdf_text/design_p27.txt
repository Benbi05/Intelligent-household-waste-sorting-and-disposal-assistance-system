字段名 数据类型主键⾮空 默认值 说明
id BIGINT 是是 AUTO_INCREMENT⽤⼾ID（物理主键）
user_type TINYINT 否是 1 ⽤⼾类型：1-居⺠，2-
管理员，3-商家
username VARCHAR(64)否是 - ⽤⼾名
phone VARCHAR(11)否是 - ⼿机号（唯⼀索引）
password VARCHAR(128
)否是 - 加密密码
avatar_url VARCHAR(255
)否否 NULL 头像URL
wechat_openi
dVARCHAR(64)否否 NULL 微信OpenID（唯⼀索
引）
status TINYINT 否是 1 状态：0-禁⽤，1-正常
created_at DATETIME否是 CURRENT_TIMESTAMP创建时间
updated_at DATETIME否是 CURRENT_TIMESTAMP
ONUPDATE更新时间
last_login_at DATETIME否否 NULL 最后登录时间
索引：
•
主键：id
•
唯⼀索引：uk_phone(phone)
•
唯⼀索引：uk_wechat_openid(wechat_openid)
•
普通索引：idx_user_type(user_type)
•
普通索引：idx_status(status)
2.Resident居⺠⽤⼾表（t_resident）
继承User基类，存储居⺠⽤⼾的扩展信息。
字段名 数据类型 主键⾮空 默认值 说明
id BIGINT 是 是 AUTO_INCREMENT记录ID
user_id BIGINT 否 是 -