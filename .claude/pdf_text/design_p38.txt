version VARCHAR(16)否 是 - 版本号（唯⼀索引）
model_path VARCHAR(255)否 是 - 模型⽂件存储路径
accuracy DECIMAL(5,2)否 否 NULL 准确率（%）
precision DECIMAL(5,2)否 否 NULL 精确率（%）
recall DECIMAL(5,2)否 否 NULL 召回率（%）
map_value DECIMAL(5,2)否 否 NULL mAP@0.5值（%）
category_cou
ntINT 否 是 0 ⽀持品类数量
dataset_size INT 否 是 0 训练集样本数量
is_active TINYINT 否 是 0 是否当前⽣效：0-否，
1-是
status TINYINT 否 是 0 状态：0-训练中，1-已
上线，2-已下线，3-训
练失败
created_at DATETIME否 是 CURRENT_TIMESTAMP创建时间
updated_at DATETIME否 是 CURRENT_TIMESTAMP
ONUPDATE更新时间
索引：
•
主键：id
•
唯⼀索引：uk_version(version)
•
普通索引：idx_is_active(is_active)
•
普通索引：idx_status(status)

13.PointRule积分规则表（t_point_rule）
存储积分奖惩规则的版本配置。
字段名 数据类型 主键⾮空默认值 说明
id BIGINT 是是 AUTO_INCREMENT规则ID
category_id BIGINT 否是 -