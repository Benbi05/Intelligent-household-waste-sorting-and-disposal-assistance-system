MVC模式应⽤：系统采⽤MVC（Model-View-Controller）设计典范，将业务逻辑、数据、界⾯显
⽰分离。Model层负责处理应⽤程序数据逻辑，在数据库中存取数据；View层负责数据显⽰；
Controller层负责处理⽤⼾交互，从视图读取数据，控制⽤⼾输⼊，并向模型发送数据。
在架构的DAO层中，编写各个实体类的数据操作对象，⽤于对数据库实体对象定义⼀系列持久化
⽅法。
在Service层中，编写整个系统的业务逻辑和数据接⼝，使⽤DAO对象对数据进⾏操作，并提供对
外的API接⼝，供前端⻚⾯调⽤。
在View层中，构建两个前端显⽰界⾯，包括微信⼩程序和Vue实现的Web端界⾯。 
⼆、技术选型介绍
1、后端技术选型
Web框架：Flask（Python）
系统后端核⼼业务服务采⽤PythonFlask轻量级Web框架实现。Flask采⽤"核⼼+扩展"的极简设计理
念，其核⼼仅包含路由系统和WSGI兼容层。选型理由如下：