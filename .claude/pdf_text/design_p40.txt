report_content LONGTEXT否是 - 报告内容（JSON格式
结构化存储）
region_code VARCHAR(16)否否 NULL 统计区域
stat_month VARCHAR(7)否是 - 统计⽉份（格式：
YYYY-MM）
file_url VARCHAR(25
5)否否 NULL 导出⽂件存储URL
status TINYINT否是 0 状态：0-⽣成中，1-已
完成，2-⽣成失败
created_at DATETIME否是 CURRENT_TIMESTAM
P创建时间
completed_at DATETIME否否 NULL 完成时间
索引：
•
主键：id
•
唯⼀索引：uk_report_no(report_no)
•
普通索引：idx_report_type(report_type)
•
普通索引：idx_stat_month(stat_month)
•
普通索引：idx_region_code(region_code)
•
组合索引：idx_month_type(stat_month,report_type)

15.OperationLog操作⽇志表（t_operation_log）
记录管理员和商家的关键操作⾏为，⽀持审计追溯。
字段名 数据类型 主键⾮空默认值 说明
id BIGINT 是是 AUTO_INCREMENT⽇志ID
user_id BIGINT 否是 - 操作⼈ID
user_type TINYINT 否是 - 操作⼈类型：1-管理
员，3-商家