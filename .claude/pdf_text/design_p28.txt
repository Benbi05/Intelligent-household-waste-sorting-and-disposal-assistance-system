关联t_user.id（外键，
唯⼀索引）
real_name VARCHAR(32)否 否 NULL 真实姓名
id_card VARCHAR(18)否 否 NULL ⾝份证号
address VARCHAR(255
)否 否 NULL 居住地址
region_code VARCHAR(16)否 否 NULL 所属区域编码
binding_device
_idBIGINT 否 否 NULL 绑定的常⽤设备ID
索引：
•
主键：id
•
唯⼀索引：uk_user_id(user_id)
•
普通索引：idx_region_code(region_code)
3.Administrator管理员表（t_administrator）
继承User基类，存储管理员的扩展信息。
字段名数据类型 主键⾮空默认值 说明
id BIGINT 是 是 AUTO_INCREMEN
T记录ID
user_id BIGINT 否 是 - 关联t_user.id（外键，唯
⼀索引）
role VARCHAR(32) 否 是 'operator' ⻆⾊：super_admin,
operator,viewer
dept VARCHAR(64) 否 否 NULL 所属部⻔
索引：
•
主键：id
•
唯⼀索引：uk_user_id(user_id)
4.Merchant商家表（t_merchant）
继承User基类，存储合作商家的扩展信息。