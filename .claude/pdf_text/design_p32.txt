字段名 数据类型主键 ⾮空 默认值 说明
id BIGINT是 是 AUTO_INCREMENT账⼾ID
user_id BIGINT否 是 - 关联t_user.id（外键，
唯⼀索引）
balance INT 否 是 0 当前积分余额
total_earnedINT 否 是 0 累计获得积分
total_spent INT 否 是 0 累计消耗积分
frozen_amou
ntINT 否 是 0 冻结积分（订单待核
销）
version INT 否 是 0 乐观锁版本号
created_at DATETIME否 是 CURRENT_TIMESTAMP创建时间
updated_at DATETIME否 是 CURRENT_TIMESTAMP
ONUPDATE更新时间
索引：
•
主键：id
•
唯⼀索引：uk_user_id(user_id)
8.DeliveryRecord投放记录表（t_delivery_record）
存储每⼀次垃圾投放的完整记录，是系统最核⼼的业务数据表。
字段名 数据类型 主键⾮空默认值 说明
id BIGINT 是 是 AUTO_INCREMENT记录ID
record_no VARCHAR(32) 否 是 - 投放记录编号（唯⼀
索引）
user_id BIGINT 否 是 - 关联t_user.id（外
键）
device_id BIGINT 否 是 - 关联t_device.id（外
键）
category_id BIGINT 否 是 -