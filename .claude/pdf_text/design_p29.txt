字段名 数据类型 主键⾮空默认值 说明
id BIGINT 是是 AUTO_INCREMEN
T记录ID
user_id BIGINT 否是 - 关联t_user.id（外键，
唯⼀索引）
merchant_nameVARCHAR(64)否是 - 商家名称
business_licenseVARCHAR(64)否否 NULL 营业执照号
contact_personVARCHAR(32)否否 NULL 联系⼈
contact_phone VARCHAR(11)否否 NULL 联系电话
shop_address VARCHAR(255)否否 NULL 店铺地址
region_code VARCHAR(16)否否 NULL 所属区域编码
audit_status TINYINT 否是 0 审核状态：0-待审核，
1-已通过，2-已拒绝
audit_remark VARCHAR(255)否否 NULL 审核备注
索引：
•
主键：id
•
唯⼀索引：uk_user_id(user_id)
•
普通索引：idx_audit_status(audit_status)
•
普通索引：idx_region_code(region_code)
5.Device智能垃圾箱表（t_device）
存储智能垃圾箱终端的设备信息与运⾏状态。
字段名 数据类型 主键⾮空默认值 说明
id BIGINT 是 是 AUTO_INCREMENT设备ID
device_code VARCHAR(32)否 是 - 设备编号（唯⼀索引）
device_name VARCHAR(64)否 否 NULL 设备名称
device_secretVARCHAR(128)否 是 - 设备密钥（⽤于接⼊鉴
权）