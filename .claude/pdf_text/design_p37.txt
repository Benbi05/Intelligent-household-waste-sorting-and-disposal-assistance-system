commodity_name VARCHAR(64)否是 - 商品名称
commodity_desc VARCHAR(255)否否 NULL 商品描述
points_price INT 否是 - 兑换所需积分数
stock INT 否是 0 库存数量
sold_count INT 否是 0 已兑换数量
category VARCHAR(32)否否 NULL 商品分类
image_url VARCHAR(255)否否 NULL 商品图⽚URL
exchange_rule VARCHAR(255)否否 NULL 兑换规则说明
status TINYINT 否是 1 状态：0-已下架，1-已
上架
sort_order INT 否是 0 排序序号
created_at DATETIME否是 CURRENT_TIMESTAM
P创建时间
updated_at DATETIME否是 CURRENT_TIMESTAM
PONUPDATE更新时间
索引：
•
主键：id
•
普通索引：idx_merchant_id(merchant_id)
•
普通索引：idx_status(status)
•
普通索引：idx_category(category)
•
组合索引：idx_merchant_status(merchant_id,status)

12.RecognitionModel识别模型表（t_recognition_model）
管理垃圾识别模型的版本信息与性能指标。
字段名 数据类型 主键⾮空默认值 说明
id BIGINT 是 是 AUTO_INCREMENT模型ID
model_nameVARCHAR(64)否 是 - 模型名称