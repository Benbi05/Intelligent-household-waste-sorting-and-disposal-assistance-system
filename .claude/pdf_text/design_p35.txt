source_type VARCHAR(16)否 否 NULL 来源业务类型：
delivery,order,system
remark VARCHAR(255)否 否 NULL 备注说明
created_at DATETIME 否 是 CURRENT_TIMES
TAMP创建时间
索引：
•
主键：id
•
唯⼀索引：uk_flow_no(flow_no)
•
普通索引：idx_user_id(user_id)
•
普通索引：idx_account_id(account_id)
•
普通索引：idx_created_at(created_at)
•
组合索引：idx_user_time(user_id,created_at)

10.PointOrder积分订单表（t_point_order）
⽤⼾使⽤积分兑换商品的订单记录。
字段名 数据类型 主键⾮空默认值 说明
id BIGINT 是是 AUTO_INCREMENT订单ID
order_no VARCHAR(32)否是 - 订单编号（唯⼀索引）
user_id BIGINT 否是 - 关联t_user.id（外键）
account_id BIGINT 否是 - 关联
t_point_account.id
（外键）
commodity_idBIGINT 否是 - 关联t_commodity.id
（外键）
merchant_id BIGINT 否是 - 关联t_merchant.id
（外键）
points_cost INT 否是 - 消耗积分数
quantity INT 否是 1 兑换数量